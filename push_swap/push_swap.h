#ifndef PUSH_SWAP_H
# define PUSH_SWAP_H

#include <unistd.h>
#include <stdlib.h>

size_t	strlen(char const *s);
char	*itoa(int n);
int		atoi(const char *s);
char	**split(char const *s, char c);

#endif
